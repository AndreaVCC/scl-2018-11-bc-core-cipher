window.cipher = {
  encode: (strng, offset) => {
    /* Acá va tu código */
 
 //ejm return strng + 'Hola' + offset; //aqui tiene que estar el cifrado y el decifrado

  },
  decode: (strng, offset) => {
    /* Acá va tu código */
  }
};





